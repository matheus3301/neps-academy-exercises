#include <bits/stdc++.h>

#define endl "\n"

using namespace std;

int n, d, ans;
int vet[1000010];

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    cin >> n >> d;
    for(int i = 0; i < n; i++){
        cin >> vet[i];
    }

    
    for(int i = 0; i < n; i++){
       for(int j = 0; j < n-i; j++){

       }
    }

    return 0;
}