#include <bits/stdc++.h>
#define endl "\n"

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int hi,mi,hf,mf;
    while(true){
        cin >> hi >> mi >> hf >> mf;
        if(hi == 0 && mi == 0 && hf == 0 && mf == 0) return 0;

        
        

        
    } 

    return 0;
}