#include <bits/stdc++.h>
//#define endl "\n"

using namespace std;

int hi,mi,hf,mf;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> hi >> mi >> hf >> mf;
    while(hi != 0 && mi != 0 && hf != 0 && mf != 0){
        

        cin >> hi >> mi >> hf >> mf;
    }
    

    return 0;
}